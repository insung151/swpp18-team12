export class ClubDetail {
    constructor(
        join_due_datetime: string,
        join_link: string,
        site_link: string,
        long_description: string,
        history: string,
        hits: number,
    ) {}
}
